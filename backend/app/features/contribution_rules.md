# Contribution Rules

The rules-first classifier uses:

- Section titles
- Citation context windows
- Surface markers such as `following`, `benchmark`, `survey`, `proof`
- Contrastive phrases such as `however`, `unlike`, and `outperform`

Ambiguous cases should be promoted to MLX-backed reranking or classification
jobs rather than silently auto-approved.
